{
  "name": "pillbox-mcp",
  "version": "0.1.0",
  "description": "MCP server for Pillbox — persistent memory for AI agents",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "node --watch dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.2",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "@types/node": "^22.14.1"
  },
  "engines": {
    "node": ">=20"
  }
}
